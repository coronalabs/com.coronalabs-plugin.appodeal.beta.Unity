Sources for the plugin `plugin.appodeal.beta.Unity`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.Unity" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
